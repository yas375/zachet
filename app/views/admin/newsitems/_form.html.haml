- f.inputs do
  = f.input :title
  = f.input :teaser, :input_html => {:rows => 8}
  = f.input :body
  = f.input :published, :required => false
  = f.input :commented, :required => false
  = f.input :colleges, :required => false
  - unless f.object.new_record?
    = f.input :created_at, :required => false
= f.buttons
