- render :layout => 'admin/colleges/layout' do
  %h2.title Создание конспекта
  .inner
    - semantic_form_for @resource, :url => admin_college_synopses_path, :html => { :multipart => true } do |f|
      = render :partial => "form", :locals => {:f => f}
