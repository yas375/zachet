- render :layout => 'admin/colleges/layout' do
  %h2.title Редактирование шпоры
  .inner
    - semantic_form_for @resource, :url => admin_college_crib_path(@resource.college, @resource) do |f|
      = render :partial => "form", :locals => {:f => f}
