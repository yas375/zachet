.block
  .content
    %h2.title Новый пользователь
    .inner
      - semantic_form_for @user, :url => admin_users_path, :html => { :multipart => true } do |f|
        = render :partial => "form", :locals => {:f => f}
