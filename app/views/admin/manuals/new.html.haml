- render :layout => 'admin/colleges/layout' do
  %h2.title Создание методы
  .inner
    - semantic_form_for @resource, :url => admin_college_manuals_path do |f|
      = render :partial => "form", :locals => {:f => f}
